# Architecture Decision Records

ADRs are chronological and immutable. To revise a decision, write a new ADR
that supersedes the old one — don't edit the original. Each file's `Status:`
line records whether the decision is currently `Accepted`, `Superseded by
ADR-NNNN`, or `Deprecated`.
